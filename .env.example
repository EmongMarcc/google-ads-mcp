# Optional: set these to pre-configure a default account for your own use
# (not needed for the public web app — credentials are entered via the UI)
GOOGLE_ADS_CLIENT_ID=
GOOGLE_ADS_CLIENT_SECRET=
GOOGLE_ADS_REFRESH_TOKEN=
GOOGLE_DEVELOPER_TOKEN=
GOOGLE_CUSTOMER_ID=
GOOGLE_MANAGER_ACCOUNT_ID=
PORT=3000
